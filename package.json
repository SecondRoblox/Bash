{
  "name": "Bash",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "vercel-build": "npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.x.x",
    "socket.io": "^4.x.x" // include other dependencies as needed
  },
  "devDependencies": {
    // any dev dependencies you might have
  },
  "engines": {
    "node": "14.x" // or your preferred Node.js version
  }
}
